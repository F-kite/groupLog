export type UserProps = {
  role: string;
  userName: string;
  userEmail: string;
};

export type BaseUserInfoProps = {
  currentGroup:string;
  currentWeeksNumber:number;
  currentDate:string;
}
